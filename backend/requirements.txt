# Core
fastapi>=0.128.0
uvicorn>=0.39.0
pydantic>=2.12.0
httpx>=0.28.0

# LLM Providers
openai>=2.29.0
anthropic>=0.49.0

# Database
psycopg[binary]>=3.2.0
psycopg_pool>=3.2.0

# Evaluation (not needed in production Docker image)
# pip install ragas datasets

# Ingestion (CFPB scraping is done with httpx + regex, no extra deps needed)
